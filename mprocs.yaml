procs:
  mobile:
    shell: cd mobile && npm start
  backend:
    shell: bin/backend
  localtunnel:
    shell: npx localtunnel --subdomain ostrich-talks --port 3000
  docker-compose:
    shell: |
      docker-compose up -d && echo "✅ Docker containers started!"
  mongodb-shell:
    shell: bin/mongosh
  mongo-express:
    shell: bin/mongo-express-sh 